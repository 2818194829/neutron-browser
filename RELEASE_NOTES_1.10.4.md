## 新特性与修复

### 网页视频全屏全面修复
- 修复全屏退出后窗口尺寸无法恢复的问题：普通窗口退出全屏后直接回到原始窗口大小，不再停留在全屏尺寸
- 修复最大化窗口全屏退出后「还原」尺寸异常的问题（点击还原按钮不再得到全屏大小）
- 修复退出全屏时「先最大化再退回原始大小」的跳变卡顿，现在一步到位、过渡流畅
- 优化全屏进出过渡性能（全屏时切换不透明背景、视图布局去重，减少卡顿）
- 完整覆盖各类全屏路径：网页 HTML5 全屏、iframe 内嵌播放器（如 B 站、爱奇艺）、旧前缀全屏 API、F11 与菜单全屏

### 扩展弹窗（Popup）修复
- 修复扩展弹窗视图异常销毁后「点击弹窗内部无反应」的问题（Can't add a destroyed child view）
- 弹窗视图销毁后自动重建，弹窗正常置顶与响应，不再出现打开后无法点击

### 架构升级
- 渲染层代码模块化：主逻辑拆分为 15 个独立模块（书签面板、下载面板、历史面板、扩展工具栏、右键菜单、快捷键等），结构更清晰、更易维护
- 共享模块迁移到 TypeScript（常量、图标解析、站点元数据、类型定义）
- 存储系统升级为 SQLite：首次启动自动从 JSON 迁移（书签、历史、下载、设置、扩展），数据更稳定可靠
- 启动脚本修复编码问题，构建后自动启动浏览器

## 下载

- Windows 安装包：Neutron-Browser-Setup-1.10.4.exe
- 安装包大小：约 100 MB
