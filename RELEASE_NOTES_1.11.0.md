## 新特性

### 垂直标签栏
- 工具栏新增垂直标签栏按钮，一键在顶部标签栏与左侧垂直标签栏之间切换
- 垂直模式下标签页在窗口左侧纵向排列，充分利用宽屏空间
- 布局状态自动记忆，重启后保持

### 标签分组
- 支持将标签页创建为分组（右键标签页操作），分组带名称、颜色圆点与标签数量
- 可折叠 / 展开分组、重命名、设置颜色、移出分组、关闭整个分组
- 分组头点击折叠，折叠后仅显示分组标题，界面更清爽

### 分屏显示
- 工具栏分屏按钮：同一窗口左右并排显示两个标签页，中间留 2px 缝隙
- 分屏右侧标签保持常驻渲染，切换标签不打断分屏视图

### 侧边栏（收藏夹）
- 工具栏新增侧边栏按钮，左侧固定展开收藏夹面板
- 点击书签直接打开页面，右上角可关闭侧边栏

### 阅读模式
- F9 一键进入沉浸式阅读模式（菜单「阅读模式」）
- 自动从当前页面提取正文（优先 article / main，兜底取文本最多的内容块），替换为干净阅读视图
- 再次按 F9 退出恢复原页面

### 内置跟踪器拦截（隐私保护）
- 新增隐私保护设置（默认开启），复用 webRequest 拦截常见广告、分析与行为跟踪器域名
- 仅拦截子资源请求（脚本 / 图片 / XHR 等），不拦截主框架导航，避免误伤用户主动访问
- 可在设置中关闭

### 无痕窗口
- 菜单「新建无痕窗口」（Ctrl+Shift+N）已实现，不再是无痕 TODO
- 无痕会话使用独立分区，不共享普通窗口的 Cookie 与浏览数据

### 将网页安装为应用
- 菜单新增「将当前页面安装为应用」：以独立窗口打开当前网站，无浏览器 UI，像原生应用一样运行
- 应用窗口内打开的新窗口 / 弹窗自动回到浏览器新标签页

## 修复

- 修复扩展弹窗打开时屏幕闪烁的问题：弹窗改为屏幕外加载、隐藏时保持尺寸移至屏幕外、仅必要时置顶，不再出现白闪与整窗重合成闪烁
- 修复 Windows 任务栏图标：设置 AppUserModelID 并显式应用窗口图标，任务栏正确显示浏览器图标而非 Electron 默认图标
- 修复程序化退出（菜单「退出」）时报 "database is not open" 的问题：数据库关闭时机从 before-quit 调整到 will-quit，等待所有窗口保存完状态后再关库
- 窗口管理重构为多窗口架构（windowManagers 数组），为无痕窗口等多窗口场景提供支持

## 下载

- Windows 安装包：Neutron-Browser-Setup-1.11.0.exe
- 安装包大小：约 100 MB
