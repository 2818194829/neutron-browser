## 新特性

### 书签栏网站图标智能识别
- 书签栏、收藏夹面板、文件夹弹窗统一使用智能图标解析：优先取历史记录与书签中的真实图标（页面 `<link rel="icon">`），逐级回退站点根图标、DuckDuckGo、DNSPod、Google
- 图标识别成功后自动缓存并回写书签，历史遗留的空图标书签在浏览一次后自动补上
- 新增同注册域子域图标信任规则，CDN 子域（如 `lf-flow-web-cdn.doubao.com`）图标可正常保存显示

### 书签对话框识别网址图标
- 添加 / 编辑书签窗口的网址输入框左侧实时显示识别到的网站图标
- 主进程抓取页面 HTML 解析真实图标，准确识别任意网站（如豆包、B 站等），国内网络可用
- 输入网址即重新识别，保存时自动将图标写入书签，书签栏同步显示

### 扩展弹窗体验修复
- 修复点击扩展造成的网页频闪（弹窗视图改为常驻隐藏，不再反复添加移除）
- 弹窗加载失败时给出明确提示，不再出现"点了没反应"
- 同扩展再次点击可关闭弹窗（对齐 Edge），切换标签页自动关闭

## 下载

- Windows 安装包：Neutron-Browser-Setup-1.10.3.exe
- 安装包大小：约 100 MB
